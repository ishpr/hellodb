{
  "name": "hellodb-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that fronts Workers AI (embeddings) and R2 (encrypted delta sync) for hellodb.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "typescript": "^5.7.3",
    "vitest": "~3.0.0",
    "wrangler": "^4.0.0"
  }
}
